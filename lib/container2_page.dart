import 'package:flutter/material.dart';

class Container2Page extends StatelessWidget {
  const Container2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Container2 widget"),
      ),
      body: Container(),
    );
  }
}
